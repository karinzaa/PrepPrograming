#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    while (num >= 0)
    {
        printf("Enter number: ");
        scanf("%d", &num);
    }
    printf("Thank You");
    return 0;
}
